import styled from "@emotion/styled"

const Styled = {
    Title: styled.h1`
        font-size: 24px;
        margin-bottom: 40px;
    `,

    List: styled.ul`
        display: flex;
        flex-wrap: wrap;
        gap: 24px;
    `
}

export default Styled